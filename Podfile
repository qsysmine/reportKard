# Uncomment the next line to define a global platform for your project
platform :ios, '10.1'

use_frameworks!

def shared_pods
  pod 'SWXMLHash', '4.0.0'
  pod 'KeychainSwift', '~> 8.0'
end
target 'ReportKard' do
  shared_pods
end
target 'ReportKardDataKit' do
  shared_pods
end
